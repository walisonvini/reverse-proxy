services:
  nginx:
    image: nginx:stable
    container_name: reverse_proxy_nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/nginx/conf.d:/etc/nginx/conf.d
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ../crypt-keeper/static:/app/cryptkeeper_static
    networks:
      - reverse_proxy

networks:
  reverse_proxy:
    external: true
    name: proxy-net
